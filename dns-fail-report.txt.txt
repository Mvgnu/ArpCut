Proof from this Mac: Now returns NXDOMAIN via the system resolver — last check



  it was leaking a real NOERROR/NODATA. Forged NXDOMAIN is landing now.

  ────────────────────────────────────────

  Block: GTA save 192.81.241.171

  Status: ❌ NOT working (regressed)

  Proof from this Mac: TCP :443 and :80 both "succeeded!" — the IP is fully

  reachable. Traceroute now routes out to the Telekom backbone (hops 2–5)

  instead of dying at the MITM like last  time.

  ────────────────────────────────────────

  Block: UDP :3111

  Status: ❌ No effect on this Mac

  Proof from this Mac: UDP:3111 egresses to the internet identically to the

  control trace — nothing is dropping it.



  What this means



  - Two DNS blocks (xbox + PSN) are solid. Good — PSN is fixed since the last

  round.

  - GTA save block broke. It was 100% blocked last time; now it's wide open (TCP

  connects succeed). When you added the new blocks, the gta-save rule dropped

  out of the firewall. Re-toggle the gta-save preset and I'll re-confirm. (The

  0% ping is just the server ignoring ICMP — TCP proves it's reachable, so don't

  trust ping alone here.)

  - UDP :3111 never took effect against this Mac. The port block lands as a rule

  on the Windows host's own port 3111 — it doesn't touch the traffic the

  Windows box is merely forwarding for the Mac, so a MITM'd device sails right

  through. As built, a port block can't block a victim's port; you'd need it

  scoped to the victim IP as a destination/forward drop, not a local-port rule.

  So: not a config slip on your end — that one won't work regardless of

  toggling.

